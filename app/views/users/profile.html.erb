<style>

</style>
<div class="container-fluid" style="padding: 0px">
  <div id="container-cover">
    <div class="hovereffect">
      <%= image_tag @user.cover.url, id: 'img-cover' %>
      <% if current_user.name == @user.name %>
          <div class="overlay">
            <h2>Actualizar foto de portada</h2>

            <%= form_for :user, url: upload_cover_path do |f| %>
                <label for="user_cover"><span class="glyphicon glyphicon-camera"></span></label>
                <%= f.file_field :cover,  class: 'inputfile', onchange: "this.form.submit();"%>
            <% end %>

          </div>
      <% end %>
    </div>
  </div>



</div>


<div id="container-avatar">
  <div class="hovereffect">
    <%= image_tag @user.avatar.url, class: 'img-responsive', id: 'img-avatar' %>
    <% if current_user.name == @user.name %>
        <div class="overlay">
          <h2>Actualizar foto de perfil</h2>
          <%= form_for :user, url: upload_photo_path do |f| %>
              <label for="user_avatar"><span class="glyphicon glyphicon-camera"></span></label>
              <%= f.file_field :avatar, class: 'inputfile', onchange: "this.form.submit();" %>
          <% end %>
        </div>
    <% end %>
  </div>
</div>

<div class="container container-stat" style="clear: both">
  <div class="row">
    <div class="col-md-3" style="height: 45px"></div>
    <div class="col-md-2"><%= link_to "Publicaciones:#{@user.posts.count}","#" %></div>
    <div class="col-md-2"><%= link_to "Seguidores:#{@user.fans.count}","#" %></div>
    <div class="col-md-2"><%= link_to "Siguiendo:#{@user.idols.count}","#" %></div>
    <div class="col-md-3">
      <% if @user.name != current_user.name %>
          <% if current_user.following?(@user) %>
              <%= form_for :relationship, url: unfollow_path, html: {method: :delete} do |f| %>
                  <%= hidden_field_tag :idol_id, @user.id %>
                  <%= f.submit "Dejar de seguir", class: 'btn btn-primary' %>
              <% end %>
          <% else %>
              <%= form_for :relationship, url: follow_path do |f| %>
                  <%= hidden_field_tag :idol_id, @user.id %>
                  <%= f.submit "Seguir", class: 'btn btn-primary  btn-block' %>
              <% end %>
          <% end %>
      <% end %>
    </div>

  </div>
</div>





<div class="container">
<div class="row">
  <div class="col-md-3">
    CONTENIDO POR ANALIZAR
  </div>
  <div class="col-md-6">
    <%= form_for :post, url: new_post_path do |f| %>
        <div class="form-group">
          <%= f.text_area(:body, class: "form-control", placeholder: 'How do you feel?') %>
          <label for="post_image"><span class="glyphicon glyphicon-camera"></span></label>
          <%= f.file_field :image, class: 'inputfile' %>
        </div>
        <%= f.submit('Publicar', class: "btn btn-primary") %>
    <% end %>
    <% @posts.each do |post| %>
        <div class="container-body ">
          <%= image_tag @user.avatar.url, id: 'img-avatar' %>
          <h5><%= post.user.name %></h5>
          <%= image_tag post.pictures.first.image.url, class: 'img-post' %>
          <p class="format-text"><%= post.body %></p>
          <button type="button" class="btn btn-default" aria-label="Left Align">
            <span class=" glyphicon glyphicon-thumbs-up" aria-hidden="true"></span>
          </button>
          <button type="button" class="btn btn-default" aria-label="Left Align">
            <span class=" glyphicon glyphicon-thumbs-down" aria-hidden="true"></span>
          </button>
          <button type="button" class="btn btn-default" aria-label="Left Align">
            <span class=" glyphicon glyphicon-share-alt" aria-hidden="true"></span>
          </button>
        </div>
    <% end %>
  </div>
  <div class="col-md-3">
    <div class="row">
      <div class="col-md-4"></div>
      <div class="col-md-4" id="sugerencia-amistad">SUGERENCIAS</div>
      <div class="col-md-4"></div>
    </div>
    <div class="row">
      <div class="col-md-4"><%= image_tag User.find_by(:id=>1).avatar.url, class: 'img-avatar' %></div>
      <div class="col-md-4"><%= image_tag User.find_by(:id=>2).avatar.url, class: 'img-avatar' %></div>
      <div class="col-md-4"><%= image_tag User.find_by(:id=>3).avatar.url, class: 'img-avatar' %></div>
    </div>
    <div class="row">
      <div class="col-md-4" style="padding: 5px 5px"><a class="btn btn-danger btn-block btn-xs" href="#" role="button">Seguir</a></div>
      <div class="col-md-4" style="padding: 5px 5px"><a class="btn btn-danger btn-block btn-xs" href="#" role="button">Seguir</a></div>
      <div class="col-md-4" style="padding: 5px 5px"><a class="btn btn-danger btn-block btn-xs" href="#" role="button">Seguir</a></div>
    </div>
    <div class="row">
      <div class="col-md-4"><%= image_tag User.find_by(:id=>4).avatar.url, class: 'img-avatar' %></div>
      <div class="col-md-4"><%= image_tag User.find_by(:id=>5).avatar.url, class: 'img-avatar' %></div>
      <div class="col-md-4"><%= image_tag User.find_by(:id=>6).avatar.url, class: 'img-avatar' %></div>
    </div>
    <div class="row">
      <div class="col-md-4" style="padding: 5px 5px"><a class="btn btn-danger btn-block btn-xs" href="#" role="button">Seguir</a></div>
      <div class="col-md-4" style="padding: 5px 5px"><a class="btn btn-danger btn-block btn-xs" href="#" role="button">Seguir</a></div>
      <div class="col-md-4" style="padding: 5px 5px"><a class="btn btn-danger btn-block btn-xs" href="#" role="button">Seguir</a></div>
    </div>
  </div>
</div>




</div>
